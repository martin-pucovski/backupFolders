@{
    # source path
    SourcePath = "C:\sourcePath"
    # destination path
    DestinationPath = "C:\destinationPath"
    # how many days should the old backups be kept
    DaysToKeep = 7
    # list all folders that should be backed up
    FolderNames = @(
                    "test"
                    "test2"
                    )
}